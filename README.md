
  # Department Achievement Tracker

  This is a code bundle for Department Achievement Tracker. The original project is available at https://www.figma.com/design/DoHhzATBSmvNWmdXvylynu/Department-Achievement-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  